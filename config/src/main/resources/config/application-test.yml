spring-boot-plus:
  # 是否启用ansi控制台输出有颜色的字体，local环境建议开启，服务器环境设置为false
  enable-ansi: false
  # 资源访问全路径前缀
  resource-access-url: http://47.105.159.10:${server.port}${server.servlet.context-path}${spring-boot-plus.resource-access-path}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_plus?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Springbootplus666!

  # Redis配置
  redis:
    database: 0
    host: localhost
    password:
    port: 6379



