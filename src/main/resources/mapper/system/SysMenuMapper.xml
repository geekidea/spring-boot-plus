<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.geekidea.boot.system.mapper.SysMenuMapper">

    <select id="getSysMenuById" resultType="io.geekidea.boot.system.vo.SysMenuInfoVo">
        select *
        from sys_menu
        where id = #{id}
    </select>

    <select id="getSysMenuList" parameterType="io.geekidea.boot.system.query.SysMenuQuery"
            resultType="io.geekidea.boot.system.vo.SysMenuVo">
        select *
        from sys_menu
    </select>

    <select id="getPermissionCodesByUserId" resultType="java.lang.String">
        select distinct m.code
        from sys_user u
                 inner join sys_user_role ur
                            on u.id = ur.user_id
                 inner join sys_role r
                            on r.id = ur.role_id
                 inner join sys_role_menu rm
                            on r.id = rm.role_id
                 inner join sys_menu m
                            on m.id = rm.menu_id
        where u.status = 1
          and r.status = 1
          and m.status = 1
          and m.code is not null
          and u.id = #{userId}
    </select>

</mapper>
